{# /etc/exports generated from resolved IPs #}
{% for export in nfs_exports %}
{{ export.path }}
{%- for ip in export.ips %}
  {{ ip }}({{ nfs.server.default_options | default("rw,sync,no_subtree_check,no_root_squash") }})
{%- endfor %}

{% endfor %}
