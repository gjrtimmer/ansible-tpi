---
- name: Configure SSH
  hosts: tpi
  gather_facts: false
  serial: 1
  roles:
    - ssh

- name: Bootstrap
  hosts: "{{ ansible_limit|default('nodes,!tpi') }}"
  gather_facts: false
  serial: 1
  roles:
    - bootstrap
